# Plan — Drift cleanup

Small fixes from the 2026-07-25 review. The repo side is **done**; what's left is
deploy actions and one verification. Delete this doc when the boxes below are checked.

## Done in git

- **Pins.** `grafana/alloy:v1.18.0` (both nodes, arm64 published so TB is fine),
  `lscr.io/linuxserver/your_spotify:1.20.0`,
  `journeyover/plex-auto-languages:1.5.4`. Only `profilarr` still floats — a parallel
  session owns that dir.
- **whoami retired.** Directory, `[[stack]]` block and Caddy route removed.
- **Telegram alerting.** Contact point + notification policy tree in
  `going-merry/observability/provisioning/alerting/contact-points.yaml`; bot token and
  chat id set in `secrets.env`.
- **your_spotify dump.** `backup.sh` in place; GM's `run.sh` globs it and the dumps dir
  is already a Backrest source, so nothing left to wire.

## Left to do — on the nodes

1. **Sync + deploy** `alloy-tb`, `alloy-gm`, `your_spotify`, `plexautolanguages`,
   `observability`, `caddy`. The pins should be no-ops (they match what's running);
   `caddy` picks up the dropped whoami route.
2. **Destroy the whoami stack in Komodo** — the sync is non-prune and will not remove
   it, and its `run_directory` no longer exists in git.
3. **Test the Telegram contact point** in Grafana (Alerting → Contact points → Test)
   once `observability` is redeployed. The policy tree will show as provisioned and
   read-only — that's expected.
4. **Verify the your_spotify dump** after the next 04:00 UTC run, on GM:
   ```bash
   ls -la /var/backups/the-sea/dumps/your_spotify-mongo.archive.gz
   ```
