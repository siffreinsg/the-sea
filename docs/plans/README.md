# Plans

In-flight work picked up from [TODO.md](../TODO.md). One file per thing,
`YYYY-MM-DD-<thing>.md`, **deleted in the commit that lands it** — whatever was durable is
by then in an ADR, a domain doc or the commit itself.

No index rows here. The live list is the directory, and every plan is linked from its TODO
item.
