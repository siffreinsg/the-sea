# TB-only: bind sshd to its own public IP, not 0.0.0.0 — matches the project's
# bind-address rule (docs/domains/networking.md § Binds), applied to sshd for the
# first time here. Port stays 22, the documented open port (docs/REFERENCE.md).
AddressFamily inet
ListenAddress 141.253.109.196
AllowUsers ubuntu
