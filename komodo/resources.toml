# Komodo resource sync. Servers are NOT declared here: both were created by the
# Periphery outbound-onboarding flow (T7) and are already connected. Redeclaring
# them with an inbound `address` would fight that model. The stack just references
# the onboarded server by name.
#
# Keep this sync in NON-destructive mode (don't let it prune unmanaged resources),
# or it may remove the onboarded servers.

[[stack]]
name = "backrest-tb"
[stack.config]
poll_for_updates = true
server = "thriller-bark"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "thriller-bark/backrest"
pre_deploy.command = "umask 077 && sops -d secrets.rclone.conf > rclone.conf"

[[stack]]
name = "backrest-gm"
[stack.config]
poll_for_updates = true
server = "going-merry"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "going-merry/backrest"
pre_deploy.command = "umask 077 && sops -d secrets.rclone.conf > rclone.conf"

[[stack]]
name = "observability"
[stack.config]
poll_for_updates = true
server = "going-merry"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "going-merry/observability"
pre_deploy.command = "umask 077 && sops -d secrets.env > .env"
extra_args = ["--force-recreate"]

[[stack]]
name = "alloy-tb"
[stack.config]
poll_for_updates = true
server = "thriller-bark"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "thriller-bark/alloy"
extra_args = ["--force-recreate"]

[[stack]]
name = "alloy-gm"
[stack.config]
poll_for_updates = true
server = "going-merry"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "going-merry/alloy"
extra_args = ["--force-recreate"]

[[stack]]
name = "alloy-baratie"
[stack.config]
poll_for_updates = true
server = "baratie"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "baratie/alloy"
extra_args = ["--force-recreate"]

[[stack]]
name = "headscale"
[stack.config]
poll_for_updates = true
server = "thriller-bark"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "thriller-bark/headscale"
extra_args = ["--force-recreate"]

[[stack]]
name = "authelia"
[stack.config]
poll_for_updates = true
server = "thriller-bark"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "thriller-bark/authelia"
pre_deploy.command = "umask 077 && sops -d secrets.users_database.yml > users_database.yml && sops -d secrets.oidc.yml > oidc.yml && sops -d secrets.env > .env"
extra_args = ["--force-recreate"]

[[stack]]
name = "dawarich"
[stack.config]
poll_for_updates = true
server = "going-merry"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "going-merry/dawarich"
pre_deploy.command = "umask 077 && sops -d secrets.env > .env"

[[stack]]
name = "your_spotify"
[stack.config]
poll_for_updates = true
server = "going-merry"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "going-merry/your_spotify"
pre_deploy.command = "umask 077 && sops -d secrets.env > .env"

[[stack]]
name = "plexautolanguages"
[stack.config]
poll_for_updates = true
server = "going-merry"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "going-merry/plexautolanguages"
pre_deploy.command = "umask 077 && sops -d secrets.env > .env"

[[stack]]
name = "profilarr"
[stack.config]
poll_for_updates = true
server = "going-merry"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "going-merry/profilarr"
pre_deploy.command = "umask 077 && sops -d secrets.env > .env"

[[stack]]
name = "cleanuparr"
[stack.config]
poll_for_updates = true
server = "going-merry"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "going-merry/cleanuparr"

[[stack]]
name = "caddy"
[stack.config]
poll_for_updates = true
server = "thriller-bark"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "thriller-bark/caddy"
pre_deploy.command = "umask 077 && sops -d secrets.env > .env"
extra_args = ["--force-recreate", "--build"]

[[stack]]
name = "gm-relay"
[stack.config]
poll_for_updates = true
server = "thriller-bark"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "thriller-bark/gm-relay"
extra_args = ["--force-recreate"]   # Caddyfile is bind-mounted

[[stack]]
name = "actualbudget"
[stack.config]
poll_for_updates = true
server = "thriller-bark"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "thriller-bark/actualbudget"
pre_deploy.command = "umask 077 && sops -d secrets.env > .env"

[[stack]]
name = "leantime"
[stack.config]
poll_for_updates = true
server = "thriller-bark"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "thriller-bark/leantime"
pre_deploy.command = "umask 077 && sops -d secrets.env > .env"

[[stack]]
name = "n8n"
[stack.config]
poll_for_updates = true
server = "thriller-bark"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "thriller-bark/n8n"

[[stack]]
name = "litellm"
[stack.config]
poll_for_updates = true
server = "thriller-bark"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "thriller-bark/litellm"
pre_deploy.command = "umask 077 && sops -d secrets.env > .env"
extra_args = ["--force-recreate"]   # config.yaml is bind-mounted

[[stack]]
name = "tika"
[stack.config]
poll_for_updates = true
server = "thriller-bark"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "thriller-bark/tika"

[[stack]]
name = "open-webui"
[stack.config]
poll_for_updates = true
server = "thriller-bark"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "thriller-bark/open-webui"
pre_deploy.command = "umask 077 && sops -d secrets.env > .env"

[[stack]]
name = "playwright"
[stack.config]
poll_for_updates = true
server = "going-merry"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "going-merry/playwright"
extra_args = ["--force-recreate", "--build"]

[[stack]]
name = "karakeep"
[stack.config]
poll_for_updates = true
server = "going-merry"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "going-merry/karakeep"
pre_deploy.command = "umask 077 && sops -d secrets.env > .env"

[[stack]]
name = "syncthing"
[stack.config]
poll_for_updates = true
server = "thriller-bark"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "thriller-bark/syncthing"

[[stack]]
name = "home-assistant"
[stack.config]
server = "going-merry"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
run_directory = "going-merry/home-assistant"

# These keep /opt/the-sea — the path every backup.sh, systemd unit and runbook
# uses — in sync with main, so nobody has to `git pull` by hand on the nodes.
# `path` is explicit: without it Komodo clones into its own repo dir instead.
[[repo]]
name = "the-sea-tb"
[repo.config]
server = "thriller-bark"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
path = "/opt/the-sea"
webhook_enabled = true

[[repo]]
name = "the-sea-gm"
[repo.config]
server = "going-merry"
git_account = "siffreinsg"
repo = "siffreinsg/the-sea"
branch = "main"
path = "/opt/the-sea"
webhook_enabled = true
