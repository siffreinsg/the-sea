# Decrypted secrets never get committed
*.dec
*.env.plain
.env
# On-the-fly sops decryption from the editor, written next to the encrypted file.
# No leading slash, so both match at any depth.
.decrypted~*
*.decrypted
rclone.conf
users_database.yml
oidc.yml
# Rendered from secrets.settings.yml by the searxng stack's pre_deploy.
going-merry/searxng/config/settings.yml

# age private keys
keys.txt
*.age.key

# runtime data / volumes
**/data/
**/*_data/
**/letsencrypt/
**/dawarich_*/

# OS noise
.DS_Store

docs/HANDOFF.md

# HA inventory export, secrets inside, not meant for git
ha-exports-temp
