services:
  plexautolanguages:
    image: journeyover/plex-auto-languages:1.6.0
    container_name: plexautolanguages
    restart: unless-stopped
    env_file: .env
    environment:
      TZ: Europe/Paris
    volumes:
      - config:/config
      - logs:/logs

volumes:
  config:
  logs:
